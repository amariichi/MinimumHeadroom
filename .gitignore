# Node / logs
node_modules/
npm-debug.log*
*.log
esp-web-tools-logs.txt

# Python caches / virtualenv
__pycache__/
.pytest_cache/
.venv/
.venv-platformio/
.venv-vllm/
.venv-qwen-tts/
tts-worker/.venv/
asr-worker/.venv/

# Local tool configs and caches
.codex_/
.claude/
.playwright-cli/
.cache/huggingface/
.cache/vllm/
.env
.env.*
.agent/
AGENTS.md
CLAUDE.md
GEMINI.md
INSTRUCTIONS.md
AGENTS.local.md
CLAUDE.local.md
GEMINI.local.md

# Large local model assets (downloaded manually)
assets/kokoro/kokoro-v1.0.onnx
assets/kokoro/voices-v1.0.bin

# Vision dev-tool scratch — live camera captures and probe output may contain
# personal frames; never track them. The runtime frame cache lives outside the
# repo (~/.cache/minimum-headroom/vision) and is not affected by this.
vision-worker/tools/real-frames/
vision-worker/tools/probe-report.md

# Generated closed-loop reports
test/tts-asr-loop/latest-*.md

# Release firmware build output (scripts/build-release-firmware.sh) — binaries, not source.
# Leading slash = repo root only, so it does NOT match the tracked mcp-server/dist or face-app/dist.
/dist/
