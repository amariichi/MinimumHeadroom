# CODEX USAGE CHECK v1.0 — PRELIMINARY

## A. SUMMARY

- Prompt version: CODEX USAGE CHECK v1.0.
- Report identifier: UC-f1647ea0cb82
- UTC date: 2026-09-06
- Outcome: **PARTIALLY FIXED**
- 症状: 不要なCodex使用枠消費、および放置後に見える使用枠変化の調査依頼。具体的な発症日時・前後の表示値は未提供。放置時の消費は未再現。
- 確認した指示の二重読み込みを修正。課金・集計問題が直ったとは判断していない。

## B. ENVIRONMENT

利用者は主にCLIを使用すると確認。主対象は独立CLI、デスクトップ・IDEは併用影響を確認する補助的な棚卸しとした。対象はHost-AとProject-A。全履歴検索は行わず、更新日時順の直近5タスク（T1〜T5）と関連設定だけを確認した。

| 項目 | 確認結果 |
|---|---|
| OS | Ubuntu 24.04.4 LTS、Linux 7.0.0-30-generic、x86_64 |
| 独立CLI | codex-cli 0.153.4。T1の開始時記録は0.153.3 |
| デスクトップ | インストール済みパッケージ chatgpt 26.825.51511。同梱Codexは0.151.0-alpha.7.2 |
| IDE | VS Code 1.108.2。登録済みOpenAI拡張26.901.22334。旧26.825.51511ディレクトリも存在するが、二重実行の証拠ではない |
| その他 | Antigravity CLI 1.1.27、Claude Code 2.1.263。Piはskillsディレクトリのみ確認し実行クライアント未確認。T3 Codeは確認範囲では見つからず |
| 認証・プラン | 読み取り専用account/readでchatgpt / plusを確認。今回確認したCLIはChatGPT Plusの使用枠。別請求APIの利用はこの認証では確認されず |
| CLI有効設定 | gpt-6-astra、reasoning medium、service_tier default（Standard）。config/readで確認 |
| タスク別 | T1は過去にxhigh/high/medium、T2〜T4はmedium、T5はxhigh。モデルは共通。設定は変更していない |
| 速度の限界 | CLIの既定と有効設定はStandard。過去の個別リクエストの速度値は記録から確定できず。デスクトップの進行中タスク設定も未確定 |
| コンテキスト | 手動context-window/auto-compaction上書きなし。直近記録のmodel_context_windowは258400。T1には5回のcompaction記録 |
| 他クライアント | ホストでCodexプロセス2個とClaudeを観測。開いているだけでは消費とみなさず、停止していない。モバイル接続歴のフラグあり。別端末・他アカウントの活動は未観測 |

デスクトップと独立CLIは別ランタイムとして扱った。Fastはモデルにより使用量倍率が異なるが、今回は有効化の証拠がなく、速度設定を変更していない。[公式速度設定](https://learn.chatgpt.com/docs/agent-configuration/speed)

## C. EVIDENCE

時刻はUTC。以下の割合は**使用済み**であり残量ではない。一次枠は300分、二次枠は10080分（週）。同一枠の観測値でも、同じアカウントの別タスクを含みうるため、そのタスク単独の課金量とは解釈しない。

| 日時・区間（2026-09-06） | ローカル証拠・使用枠 |
|---|---|
| 04:14〜08:11 | T5に2回の実行。一次63→13%はリセット時刻が変わっており単純差分不可。週75→83% |
| 14:08:24〜14:08:30 | T4は中断。使用枠19% / 3%。response-usage記録は0件。欠測を消費ゼロと断定しない |
| 14:11:46〜14:12:54 | T3完了。一次23→34%、週4→5%。一次リセット18:29:55、週リセット09-13 13:29:55 |
| 14:16:54〜14:26:34 | T2に2回の実行。一次43→79%、週7→12%。T1と活動が重なる |
| 14:26:38〜19:31:10 | T1のresponse-usage記録に約305分の空白。ただしタスクは継続状態で、連続的な使用枠観測もなく、無消費の証明にはならない |
| 21:44:08 | T1の調査前の最終完了。一次55%、週21%。以後、調査開始までの連続使用枠データなし |
| 23:25:00 | 今回の調査開始。これ以降の活動を放置検証から除外 |
| 23:46:29〜23:46:30 | 読み取り専用probe成功。一次92%、週27%。**この増加区間には本調査が含まれる**。放置後消費の証拠ではない |

probe時の一次リセットは2026-09-07 00:31:10 UTC、週リセットは2026-09-13 13:29:55 UTC。購入クレジットはhasCredits=false、balance=0、unlimited=false。購入クレジット、サブスクリプション枠、API課金は別の指標として扱う。[使用枠フィールドの公式定義](https://learn.chatgpt.com/docs/app-server)

調査開始前のresponse-usage記録はT1=385、T2=13、T3=7、T4=0、T5=18件。同一タスク内のresponse_id重複は見つからなかった。これは異なるIDを持つ再送の不存在までは証明しない。記録時刻はレスポンス使用量の記録時点であり、サーバーへの送信開始時刻ではない。実際の送信時刻・表示更新時刻を完全に結び付けるテレメトリーは不足している。

T2の入力547166のうち484608、T3は242394のうち206336、T5は1009562のうち880128がcached input。キャッシュは利用されている。cached inputを入力に再加算せず、reasoningをoutputに再加算せず、累積カウンター同士も合算していない。ローカルトークン数から課金クレジットを換算していない。文脈量・推論・キャッシュなどが使用量に影響する。[公式使用量説明](https://learn.chatgpt.com/docs/pricing)

ローカルqueueは0件、goalは1件でcomplete。独自hooks.jsonとCodex automationsディレクトリはなし（hooks機能自体は有効）。別CLI関連の定期タイマー4件を確認したが、Codexの重複実行と断定する証拠はない。プラグイン内部・リモート自動処理すべてを網羅した検証ではない。不要と確認できない処理は停止していない。標準の要約メモリファイル2候補は存在しなかったが、他経路のメモリ不存在は意味しない。

## D. FIXES

### F1 — CONFIRMED: 指示の重複

全体指示14984文字とプロジェクト指示が、操作通知・再起動・helper運用規則をほぼ重複して注入していた。両方の原本をバックアップ後、全体指示を1040文字の必須ルーティングに整理した。プロジェクト側には共有規則の網羅を表すマーカーだけを追加した。

同じ規則がプロジェクト指示に既に含まれる場合はその規則を使い、その他のプロジェクトでは保存した完全な全体指示を一度読み込む。規則自体、モデル、推論、認証、履歴、既存作業は維持。共有プロジェクト指示の本文を残し、他クライアントにも配慮した。

検証: 元の全体指示を別ファイルに全文保存したこと、および説明用の日本語括弧を除いて元の共有規則全行がプロジェクトに存在することを機械比較。対象プロジェクトの注入文字数は差し引き13879文字減少。新しいモデルタスクを起動しての節約測定は行っていない。既存会話に入った過去の指示は消えない。

ロールバック: ローカル監査用backupsのglobal-AGENTS.mdを全体AGENTS.mdへ、project-AGENTS.mdをプロジェクトAGENTS.mdへ戻す。元のモデル設定に変更はない。詳細な復元先と操作は共有対象外のrollback.mdに保存。

### F2 — UNKNOWN: 放置中の継続消費

中断後のモデル継続、不要な二重スケジュール、孤立したモデル処理は確認範囲で立証できなかった。遅延集計・別端末・表示不具合は候補にとどまる。根拠なくプロセス停止やキャッシュ消去をしなかった。

対策として読み取り専用の観測スクリプトを追加した。モデル開始APIは許可リストで拒否。8項目のローカル検証と実際のaccount/config/rateLimits読み取りが成功。サンドボックス内の初回probeはタイムアウトしたが、通常環境では1.5秒で成功した。削除すれば観測機能の追加を戻せる。自動起動・定期実行への登録はしていない。

## E. IDLE OBSERVATION

**Status: NOT RUN / PRELIMINARY**。実際のアイドル観測時間0分。新規要求数・放置時の使用枠変化は未測定。上記probeは75分観測の代用ではない。

この回答と既存の全モデル作業が完了した後、他の観測可能な端末でも同じアカウントの作業がないことを確認して、Project-Aの通常ターミナルで実行する:

```sh
python3 scripts/codex-usage-observe.py --minutes 75 --confirm-idle
```

ターミナルを開いたまま75分間待つ。スクリプトは1分ごとに読み取り専用使用枠を取得し、選定済み5タスクの追記だけを監視する。新しいローカルスレッドはDBの作成時刻メタデータで件数だけ確認する。モデル要求・AIの定期起床は行わない。過去の本文を再走査しない。

結果はローカル監査フォルダの時刻付きidle-observation JSONへ毎回保存され、終了時にも自動保存される。途中中断はINTERRUPTED、テレメトリー失敗は明記する。別の時間窓に切り替わった使用率は単純減算しない。新規response記録またはタスク開始を検出した区間は干渉ありとして扱う。要求開始記録ではないため、未記録の送信中要求は検出できない。

カバー範囲は選定5タスクのローカル記録と当該認証で得られる使用枠。ほかの既存スレッド・端末・別認証クライアントの不活動は保証しない。開始前に保留中の作業を完了させる必要がある。75分内の枠リセット、OSスリープ、ネットワーク切断は比較に影響する。共有報告書は観測前のまま残り、観測結果ファイルだけがAIなしで更新される。

## F. CONCLUSION

検証済みなのは、重複した指示の解消、CLI Standard設定、ChatGPT Plus認証、入力キャッシュの再利用、読み取り専用probeと観測スクリプトの検証。期待される効果は今後の対象プロジェクトの不要な入力文脈削減であり、使用枠の節約率は未測定。

放置後の減少原因や課金問題は未確定。次に必要なのは、同じリセット窓での75分以上の連続観測、開始前に作業が終わっている証拠、他クライアント・端末の利用有無、および問題を見た表示の時刻。現状で集計不具合や課金修復を断言しない。

共有用の本文から認証情報、個人・アカウント識別子、メール、プロンプト、実タスク名、リポジトリ名、IP、個人パスを除去した。元の限定的な証拠とバックアップはローカルに保存し、外部送信はしていない。
